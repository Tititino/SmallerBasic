package smallerbasic.AST.nodes;

/**
 * An {@link ASTNode} representing an expression.
 * An expression is anything that can be evaluated to a value.
 */
public interface ExpressionASTNode extends ASTNode {}
